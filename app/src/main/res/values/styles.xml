<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowSoftInputMode">stateHidden|adjustPan</item>
        <item name="android:windowBackground">@color/bg</item>
    </style>

    <!--Dialog样式-->
    <style name="BaseDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="BottomToTopDialogTheme" parent="@style/BaseDialogTheme">
        <item name="android:minWidth">10000dp</item>
        <item name="android:windowAnimationStyle">@style/BottomToTopDialogAnimation</item>
    </style>

    <style name="BottomToTopDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/dl_b2t_show</item>
        <item name="android:windowExitAnimation">@anim/dl_b2t_hidden</item>
    </style>

    <!--分割线样式-->
    <style name="DividerVertical">
        <item name="android:layout_width">@dimen/divider</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/divider</item>
    </style>

    <style name="DividerHorizontal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/divider</item>
        <item name="android:background">@color/divider</item>
    </style>
</resources>
